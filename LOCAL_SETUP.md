# 🚀 Инструкция по локальному запуску проекта

## ✅ Быстрый ответ на ваши вопросы:

1. **Да, сокомандиник может исправить конфликты через GitHub** - он может сделать pull, исправить конфликты и запушить
2. **Да, сайт работает БЕЗ чат-бота** - это в требованиях было! Frontend и API туров работают независимо
3. **Да, чат-бот будет работать** - если есть OpenAI API key

---

## 📋 Пошаговая инструкция

### Шаг 1: Клонировать репозиторий (если еще не склонирован)

```bash
git clone https://github.com/AgytaiMukhatai/tour-booking.git
cd tour-booking
```

### Шаг 2: Запустить Backend

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
npm install

# Создать .env файл (скопировать из примера)
# Windows:
copy env.example.txt .env
# Linux/Mac:
# cp env.example.txt .env

# Отредактировать .env и добавить OpenAI API key:
# OPENAI_API_KEY=sk-your-key-here

# Запустить сервер
npm run dev
```

Backend запустится на **http://localhost:3001**

**Важно:** 
- Если нет OpenAI API key - сайт все равно работает, просто чат-бот не будет работать
- API туров (`/api/tours`) работает без OpenAI

### Шаг 3: Запустить Frontend (в новом терминале)

```bash
# Вернуться в корень проекта
cd ..

# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить frontend
npm run dev
```

Frontend запустится на **http://localhost:5174** (или другой порт, который покажет Vite)

---

## 🎯 Что работает:

### ✅ Без OpenAI API key:
- ✅ Просмотр списка туров
- ✅ Фильтрация туров
- ✅ Детали тура
- ✅ Бронирование тура
- ✅ Все UI компоненты

### ✅ С OpenAI API key:
- ✅ Все вышеперечисленное
- ✅ AI чат-бот для подбора туров
- ✅ Умные рекомендации

---

## 🔧 Проверка что все работает:

### Backend:
1. Откройте в браузере: http://localhost:3001/health
2. Должен быть ответ: `{"status":"ok","message":"Tour Booking API is running"}`

### Frontend:
1. Откройте в браузере: http://localhost:5174
2. Должна открыться главная страница с турами

### API туров:
1. Откройте: http://localhost:3001/api/tours
2. Должен вернуться JSON с турами

### AI чат (если есть API key):
1. Откройте frontend
2. Найдите кнопку/раздел с AI чатом
3. Отправьте сообщение типа "Хочу поехать в Японию"

---

## ⚠️ Если что-то не работает:

### Backend не запускается:
- Проверьте что порт 3001 свободен
- Проверьте что установлен Node.js 18+
- Проверьте что все зависимости установлены (`npm install`)

### Frontend не запускается:
- Проверьте что порт 5174 свободен
- Проверьте что установлен Node.js 18+
- Проверьте что все зависимости установлены (`npm install`)

### Чат-бот не отвечает:
- Проверьте что `OPENAI_API_KEY` указан в `.env` файле
- Проверьте что backend запущен
- Проверьте консоль backend на ошибки

---

## 📝 Структура проекта:

```
tour-booking/
├── frontend/          # React приложение
│   └── npm run dev    # Запуск на :5174
├── backend/           # Express API
│   └── npm run dev    # Запуск на :3001
└── README.md          # Основная документация
```

---

## 🎉 Готово!

Теперь у вас работает:
- ✅ Frontend с турами
- ✅ Backend API
- ✅ AI чат-бот (если есть API key)

**Помните:** Сайт работает БЕЗ чат-бота! Чат-бот - это дополнительная функция для улучшения UX.

