# 🚀 Быстрый старт AI Chat Bot

## Шаг 1: Установка зависимостей

```bash
cd backend
npm install
```

## Шаг 2: Настройка OpenAI API

1. Создайте файл `.env` в папке `backend/`
2. Скопируйте содержимое из `env.example.txt`
3. Получите API ключ OpenAI: https://platform.openai.com/api-keys
4. Вставьте ключ в `.env`:
   ```
   OPENAI_API_KEY=sk-your-actual-key-here
   ```

## Шаг 3: Запуск

```bash
npm run dev
```

Сервер запустится на `http://localhost:3001`

## Шаг 4: Тестирование

Откройте другой терминал и протестируйте:

```bash
curl -X POST http://localhost:3001/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Хочу поехать в Японию, бюджет до 3000 долларов"}'
```

Или используйте Postman/Insomnia для тестирования.

## ✅ Готово!

Теперь ваш AI Chat Bot готов к работе!

**Что дальше:**
- Настройте MCP серверы (опционально)
- Интегрируйте с фронтендом
- Настройте промпт под ваши нужды

См. [SETUP.md](./SETUP.md) для подробной информации.

